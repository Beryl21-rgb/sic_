App installation:
VS code

GIT configuration:
git config --global user.name "Beryl21-rgb"
global user.email "beryltbinu120@gmail.com"

 
organising(arranging) files and folders:
cloning a repo:
important git commands:

GIT congiguration
to update my remote repo w.r.t local repo
1.let the git and the files that need to be updated
2.let the git commit changes

1.stage  
stage - list of files that needs to be updated
commit-files will be copied
push

the local repo should be uopdated first (pull operation)